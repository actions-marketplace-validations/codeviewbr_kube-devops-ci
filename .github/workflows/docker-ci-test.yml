name: kube-devops-ci-test

on: 
  pull_request:
    branches:
      - main

jobs:
  build_test_docker_image:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      - 
        name: Set output
        id: vars
        run: echo ::set-output name=tag::${GITHUB_REF#refs/*/}
      - 
        name: Test Build Image
        run: docker build -t kube-devops-ci:test .